- job:
    name: logstash-input-bugzilla_infra-image-build
    project-type: flow
    description: |
      Building, testing and deploying Docker images.
    display-name: '[LIP-Bugzilla] Infrasructure'
    concurrent: false
    triggers:
      - gerrit:
          trigger-on:
              - patchset-created-event
          projects:
              - project-compare-type: 'PLAIN'
                project-pattern: 'abraverm/logstash-input-bugzilla'
                branches:
                  - branch-compare-type: 'PLAIN'
                    branch-pattern: 'ci'
                file-paths:
                  - compare-type: 'ANT'
                    pattern: '**/docker/**'
          skip-vote:
              unstable: true
              notbuilt: true
          trigger-for-unreviewed-patches: true
    node: master
    dsl: |
      guard {
        build("logstash-input-bugzilla_docker-build-node",
              gerrit_refspec: params["GERRIT_REFSPEC"],
              deploy: build.number
                )
        parallel (
          {
            build("logstash-input-bugzilla_docker-build-elk-host",
                  gerrit_refspec: params["GERRIT_REFSPEC"],
                  deploy: build.number)
            build("logstash-input-bugzilla_docker-build-logstash",
                  gerrit_refspec: params["GERRIT_REFSPEC"],
                  deploy: build.number)
            build("logstash-input-bugzilla_docker-test-elk",
                  gerrit_refspec: params["GERRIT_REFSPEC"],
                  deploy: build.number)
          },
          { retry ( 2 ) { build("logstash-input-bugzilla_docker-build-jjb",
                            gerrit_refspec: params["GERRIT_REFSPEC"],
                            deploy: build.number) }}
        )
        build("logstash-input-bugzilla_docker-image-deploy", deploy: build.number)
      } rescue {
        build("logstash-input-bugzilla_docker-image-cleanup", deploy: build.number)
      }
